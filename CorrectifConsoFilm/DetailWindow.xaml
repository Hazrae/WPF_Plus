<Window x:Class="CorrectifConsoFilm.DetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CorrectifConsoFilm"
        mc:Ignorable="d"
        Title="DetailWindow" Height="250" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Titre :" />
        <TextBlock Grid.Row="1" Text="Annee de sortie :" />

        <TextBlock Grid.Column="1" Text="{Binding Titre}" />
        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Annee}" />

    </Grid>
</Window>
